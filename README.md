# DPC-Mapper
Driver manual mapper finds the least active core on the system, and launches a deffered procedure to execute the drivers entry point on the core. Takes in a allocated structure wih DPC context information
